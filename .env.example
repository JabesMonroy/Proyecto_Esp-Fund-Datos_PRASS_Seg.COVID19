# Plantilla de variables de entorno. Copiar a `.env` y ajustar.
# cp .env.example .env   (Linux/macOS)   |   copy .env.example .env   (Windows)

# Ruta al CSV crudo
DATA_RAW=data/SegCovid19-Seguimiento_PRASS.csv

# Carpeta de salida para datos procesados
DATA_PROCESSED=data/processed.parquet

# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
